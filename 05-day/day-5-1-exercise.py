#!/usr/bin/python3
student_heights = input("input a list of student height ").split()
for n in range(0, len(student_heights[n])):
   student_heights[n] = int(student_heights[n])
print(student_heights)

height_sum = 0
for height_sum in student_heights:
    height_sum += student_heights
print(height_sum)
